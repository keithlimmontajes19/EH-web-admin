import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const {{pascalCase name}} = (props: PropsType): ReactElement => {
  return <></>;
};

export default {{pascalCase name}};
