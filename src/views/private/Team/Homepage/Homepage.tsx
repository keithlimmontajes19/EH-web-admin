import { ReactElement } from "react";

import type { PropsType } from "./types";
import {} from "./styled";

const Homepage = (props: PropsType): ReactElement => {
  return <></>;
};

export default Homepage;
