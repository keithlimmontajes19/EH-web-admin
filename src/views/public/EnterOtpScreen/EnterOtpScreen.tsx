import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const EnterOtpScreen = (props: PropsType): ReactElement => {
  return <></>;
};

export default EnterOtpScreen;
