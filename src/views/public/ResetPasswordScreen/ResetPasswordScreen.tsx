import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const ResetPasswordScreen = (props: PropsType): ReactElement => {
  return <></>;
};

export default ResetPasswordScreen;
