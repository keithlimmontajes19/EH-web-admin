import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const LessonContentTreeTable = (props: PropsType): ReactElement => {
  return <></>;
};

export default LessonContentTreeTable;
