import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const ForgotPassword = (props: PropsType): ReactElement => {
  return <></>;
};

export default ForgotPassword;
