import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const ResetPassword = (props: PropsType): ReactElement => {
  return <></>;
};

export default ResetPassword;
