import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const Signup = (props: PropsType): ReactElement => {
  return <></>;
};

export default Signup;
