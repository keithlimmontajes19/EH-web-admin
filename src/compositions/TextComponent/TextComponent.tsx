import {ReactElement} from 'react';

import type {PropsType} from './types';
import {} from './styled';

const TextComponent = (props: PropsType): ReactElement => {
  return <></>;
};

export default TextComponent;
